# Only search with case sensitivity if there is mixed case.
--smart-case

# Follow symlinks.
--follow

# Some basic ignores.
--ignore-dir=coverage
--ignore-dir=tmp

# Some files not usually covered by ack.
--type-add=html=.html.erb
--type-add=js=.js.erb
--type-add=ruby=.rake,.rsel,.builder

# Some more types
--type-set=cucumber=.feature
--type-set=haml=.haml
--type-set=markdown=.markdown,.md
--type-set=slim=.slim
